const circlesPerGame = 5;
const levelsPerCircle = 16;
const levelsPerGame = circlesPerGame * levelsPerCircle;
const levelCount = 2 * levelsPerGame;
const tilesPerBlock = 2;
const pixelsPerBlock = 16;

export {
	circlesPerGame,
	levelsPerCircle,
	levelsPerGame,
	levelCount,
	pixelsPerBlock,
	tilesPerBlock,
};
