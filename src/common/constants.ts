const circlesPerGame: number = 5;
const levelsPerCircle: number = 16;
const levelsPerGame: number = circlesPerGame * levelsPerCircle;
const levelCount: number = 2 * levelsPerGame;
const tilesPerBlock: number = 2;
const pixelsPerBlock: number = 16;
const tileSize: number = 8;

export {
	circlesPerGame,
	levelsPerCircle,
	levelsPerGame,
	levelCount,
	pixelsPerBlock,
	tileSize,
	tilesPerBlock,
};
